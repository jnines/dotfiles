#!/bin/bash

# Turn off LEDs, doesn't seem to exist anymore
# nvidia-settings -a GPULogoBrightness=0

# Memory OC and Enable Fan control
# nvidia-settings -a '[gpu:0]/GPUMemoryTransferRateOffsetAllPerformanceLevels=800' -a '[gpu:0]/GPUFanControlState=1'
#
sudo nvidia-smi -lgc 210,600 &&
  sudo nvidia-smi -lmc 405,810

